<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Permissions required for audio recording on Wear OS -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
        Remove location permissions merged from client:location (transitive via client:data).
        Location tracking is not used on Wear OS.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" tools:node="remove" />

    <!-- Remove OpenGL ES requirement merged from Google Maps SDK -->
    <uses-feature android:glEsVersion="0x00020000" tools:node="remove" />

    <!-- Remove Google Maps package query merged from client:location -->
    <queries>
        <package android:name="com.google.android.apps.maps" tools:node="remove" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault"
        android:name=".LogDateWearApplication">

        <!--
            Remove auto-initializers merged from transitive dependencies.
            Firebase, Google Maps, WorkManager, and Emoji2 are not used on Wear OS
            but their ContentProviders run before Application.onCreate(), adding
            500-1500ms of startup latency on constrained Wear hardware.
        -->
        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="${applicationId}.firebaseinitprovider"
            android:exported="false"
            tools:node="remove" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <!-- Remove WorkManager (uses its own SQLite DB on main thread) -->
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
            <!-- Remove Emoji2 font download -->
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!--
            Remove location tracking components merged from client:location.
            These services/receivers are not used on Wear OS and add unnecessary
            manifest entries, boot receivers, and foreground service registrations.
        -->
        <service
            android:name="app.logdate.client.location.tracking.DetailedLocationTrackingService"
            tools:node="remove" />
        <receiver
            android:name="app.logdate.client.location.tracking.OptimizedBackgroundLocationReceiver"
            tools:node="remove" />
        <receiver
            android:name="app.logdate.client.location.tracking.LocationTrackingBootReceiver"
            tools:node="remove" />

        <!-- Remove Google Places SDK activities (not used on Wear OS) -->
        <activity
            android:name="com.google.android.libraries.places.widget.AutocompleteActivity"
            tools:node="remove" />
        <activity
            android:name="com.google.android.libraries.places.widget.BasicPlaceAutocompleteActivity"
            tools:node="remove" />
        <activity
            android:name="com.google.android.libraries.places.widget.PlaceAutocompleteActivity"
            tools:node="remove" />
        <activity
            android:name="com.google.android.libraries.places.widget.internal.photoviewer.PlacesLightboxActivity"
            tools:node="remove" />

        <!-- Audio Recording Service -->
        <service
            android:name=".recording.WearAudioRecordingService"
            android:foregroundServiceType="microphone"
            android:enabled="true"
            android:exported="false" />

        <!-- Data Layer sync listener: receives notes from paired phone -->
        <service
            android:name=".sync.WearDataLayerListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/logdate/notes" />
            </intent-filter>
        </service>
            
        <service
            android:name=".complication.MainComplicationService"
            android:exported="true"
            android:label="@string/complication_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>

            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="0" />
        </service>
        <service
            android:name=".tile.MainTileService"
            android:exported="true"
            android:label="@string/tile_label"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>

            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview" />
        </service>

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/MainActivityTheme.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>