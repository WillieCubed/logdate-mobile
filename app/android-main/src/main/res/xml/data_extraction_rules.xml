<?xml version="1.0" encoding="utf-8"?>
<!--
   Backup/restore rules for Android 12+ (API 31+).
   See https://developer.android.com/about/versions/12/backup-restore#xml-changes

   Cloud backup (Google Drive, 25 MB limit):
     KeyStore keys are NOT included. Encrypted databases and EncryptedSharedPreferences
     are useless after restore. Only DataStore preferences (lightweight state) are backed up.

   Device-to-device transfer (direct local transfer, 2 GB limit):
     Transfers everything so the app works on the new device with zero friction.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="file" path="datastore/"/>
        <exclude domain="database" path="."/>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="file" path="audio_notes/"/>
    </cloud-backup>
    <device-transfer>
        <include domain="file" path="."/>
        <include domain="database" path="."/>
        <include domain="sharedpref" path="."/>
    </device-transfer>
</data-extraction-rules>
