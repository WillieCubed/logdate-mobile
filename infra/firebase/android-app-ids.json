{
  "_comment": [
    "Firebase Android app IDs per project, keyed by application ID.",
    "These are public identifiers -- they ship inside every APK -- and are recorded here so a",
    "google-services.json that predates the studio.hypertext.logdate rename can be reconciled",
    "before validation, instead of failing every Android job.",
    "TEMPORARY. Delete this file, scripts/reconcile-google-services.sh, and the reconcile step in",
    ".github/actions/setup-firebase-configs once LOGDATE_ANDROID_GOOGLE_SERVICES_JSON_DEBUG_BASE64",
    "and _RELEASE_BASE64 carry configurations that already declare the client."
  ],
  "logdate-dev": {
    "studio.hypertext.logdate": "1:786734185325:android:d1d954e3ec8b414b23f864"
  },
  "logdate": {
    "studio.hypertext.logdate": "1:216887423795:android:1d5cb98b3aaefc568ec446"
  }
}
