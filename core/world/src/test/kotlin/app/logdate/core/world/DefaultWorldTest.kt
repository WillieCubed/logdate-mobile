package app.logdate.core.world

import kotlinx.coroutines.test.runTest
import org.junit.Test


class DefaultWorldTest {

    /**
     * When given a
     */
    @Test
    fun `ensure places can be fetched`() = runTest {
    }
}
