package app.logdate.client.device.models

/**
 * Represents the platform a device runs on.
 */
enum class DevicePlatform {
    ANDROID,
    IOS,
    MACOS,
    WINDOWS,
    LINUX,
    WEB,
    UNKNOWN
}