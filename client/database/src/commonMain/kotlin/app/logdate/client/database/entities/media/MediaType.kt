package app.logdate.client.database.entities.media

/**
 * Enum representing the type of indexed media.
 * 
 * Used to differentiate between different types of media in the database.
 */
enum class MediaType {
    IMAGE,
    VIDEO
}